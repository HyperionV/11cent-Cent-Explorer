#ifndef THEME_HPP_
#define THEME_HPP_

#include "raylib.h"

namespace theme {
	const Color foreground = RAYWHITE;
	const Color background = Color({1, 14, 28, 255});
	const Color focus = Color({218, 243, 255, 255});
	const Color text = background;
}

#endif // THEME_HPP_