#include "button.hpp"

// Button(Rectangle rec, const char* text) : rec(rec), text(text), clicked(false) {}
// Button(Rectangle rec, const std::string &text) : rec(rec), text(text), clicked(false) {}

// void Button::draw() {
// 	if (GuiButton(rec, text.c_str())) {
// 		clicked = true;
// 	} else {
// 		clicked = false;
// 	}
// }

// bool Button::isClicked() {
// 	return clicked;
// }